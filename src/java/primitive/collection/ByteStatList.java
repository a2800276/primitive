package primitive.collection;

/* DO NOT EDIT THIS FILE, IT IS GENERATED! */

import java.util.Random;

public class ByteStatList extends ByteList {

  public void each (ByteFunction func) {
    for(int i=0; i!=this.size(); ++i) {
      func.apply(this.get(i));
    }  
  }

  public byte reduce(byte initial, ByteFunction func) {
    byte memo = initial;
    for(int i=0; i!=this.size(); ++i) {
      memo = func.accumulate(memo, this.get(i));
    }
    return memo;
    
  } 

  public byte sum () {
    ByteFunction f = new ByteFunction() {
      public byte accumulate(byte memo, byte value) {
        return (byte)(memo+value);
      }
    };

    return this.reduce((byte)0,f);
  }

  public ByteStats stats () {
    
    final ByteStats stats = new ByteStats();

    ByteFunction f = new ByteFunction() {
      public void apply(byte value) {
        
        if (value < stats.min) {
          stats.min = value;
        } 
        if (value > stats.max) {
          stats.max = value;
        }
        stats.sum += value;
      }
    
    };
     
    this.each(f);
    
    stats.n   = this.size();
    stats.avg = stats.sum / (double)stats.n;
    
    f = new ByteFunction() {
      public void apply(byte value) {
        stats.var += Math.pow(stats.avg - (double)value, 2);
      }
    };
    
    this.each(f);

    stats.var  = stats.var / (double)stats.n;
    stats.stdd = Math.pow(stats.var, 0.5);
    return stats;

    
  }

  static ByteStatList random(int count) {
    Random       rand = new Random(0L);
    ByteStatList slist = new ByteStatList();
    byte value;
    for (int i=0; i!=count; ++i) {
      value = (byte)rand.nextInt(256);
      slist.add(value);
    }
    return slist;
  }

  class ByteStats {
    public int n;
    public byte min = Byte.MAX_VALUE;
    public byte max = Byte.MIN_VALUE;
    public byte sum;
    public double avg;
    public double stdd;
    public double var;
    public String toString() {
      StringBuilder b = new StringBuilder();
      b.append("n    = "); b.append(n);   b.append("\n");
      b.append("min  = "); b.append(min); b.append("\n");
      b.append("max  = "); b.append(max); b.append("\n");
      b.append("sum  = "); b.append(sum); b.append("\n");
      b.append("avg  = "); b.append(avg); b.append("\n");
      b.append("stdd = "); b.append(stdd);b.append("\n");
      b.append("var  = "); b.append(var); b.append("\n");
      return b.toString();
    }
  }
  
  private static void p(Object o) {
    System.out.println(o);
  }
  public static void main (String [] args) {
    ByteStatList list = ByteStatList.random(10);
    
    list.each(new ByteFunction() {
      public void apply(byte value) {
        p(value);
      }
    });
    p(list.stats());
    p(list.sum());

    IntStatList l = new IntStatList();
    for (int i =1; i<101; ++i) {
      l.add(i);
    }
    p(l.stats());
    p(l.sum());
  }

}

